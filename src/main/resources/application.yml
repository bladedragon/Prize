server:
  port: 80

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/prize?characterEncoding=utf8&useSSL=false&serverTimezone=UTC
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password:
  jpa:
    show-sql: true
  redis:
    host: localhost
    port: 6379
    database: 12
    password:
    timeout: 500ms

    jedis:
      pool:
        max-idle: 8
        min-idle: 2
        max-active: 8
        max-wait: 10000ms
mybatis:
  type-aliases-package: team.redrock.prize.bean


prjPath: AcceptPrize/